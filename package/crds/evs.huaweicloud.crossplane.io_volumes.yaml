---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: volumes.evs.huaweicloud.crossplane.io
spec:
  group: evs.huaweicloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - huaweicloud
    kind: Volume
    listKind: VolumeList
    plural: volumes
    singular: volume
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Volume is the Schema for the Volumes API. Manages a volume resource
          within HuaweiCloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VolumeSpec defines the desired state of Volume
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  autoPay:
                    type: string
                  autoRenew:
                    description: |-
                      Specifies whether auto-renew is enabled.
                      Valid values are true and false. Defaults to false.
                    type: string
                  availabilityZone:
                    description: Specifies the availability zone for the disk.
                    type: string
                  backupId:
                    description: Specifies the backup ID from which to create the
                      disk.
                    type: string
                  cascade:
                    description: |-
                      Specifies the delete mode of snapshot. The default value is false. All snapshot
                      associated with the disk will also be deleted when the parameter is set to true.
                    type: boolean
                  chargingMode:
                    description: |-
                      Specifies the charging mode of the disk.
                      The valid values are as follows:
                    type: string
                  dedicatedStorageId:
                    description: Specifies the ID of the DSS storage pool accommodating
                      the disk.
                    type: string
                  description:
                    description: Specifies the disk description. You can enter up
                      to 85 characters.
                    type: string
                  deviceType:
                    description: |-
                      Specifies the device type of disk to create. Valid options are VBD and
                      SCSI. Defaults to VBD.
                    type: string
                  enterpriseProjectId:
                    description: |-
                      Specifies the enterprise project ID of the disk.
                      For enterprise users, if omitted, default enterprise project will be used.
                    type: string
                  imageId:
                    description: Specifies the image ID from which to create the disk.
                    type: string
                  iops:
                    description: |-
                      Specifies the IOPS(Input/Output Operations Per Second) for the volume.
                      The field is valid and required when volume_type is set to GPSSD2 or ESSD2.
                      This field can be changed only when the disk status is Available or In-use.
                    type: number
                  kmsId:
                    description: Specifies the Encryption KMS ID to create the disk.
                    type: string
                  multiattach:
                    description: |-
                      be-expanded disk is in-use, the status of the server to which the disk attached
                      must be ACTIVE, PAUSED, SUSPENDED, or SHUTOFF.
                      5. Please refer to Notes and Constraints
                      to view the limitations of disk capacity expansion.
                    type: boolean
                  name:
                    description: Specifies the disk name. You can enter up to 64 characters.
                    type: string
                  period:
                    description: Specifies the charging period of the disk.
                    type: number
                  periodUnit:
                    description: |-
                      Specifies the charging period unit of the disk.
                      Valid values are month and year. This parameter is mandatory if charging_mode is set to prePaid.
                    type: string
                  region:
                    description: |-
                      Specifies the region in which to create the disk. If omitted, the
                      provider-level region will be used. Changing this parameter will create a new resource.
                    type: string
                  serverId:
                    description: |-
                      Specifies the server ID to which the cloud volume is to be mounted.
                      After specifying the value of this field, the cloud volume will be automatically attached on the cloud server.
                      The charging_mode of the created cloud volume will be consistent with that of the cloud server.
                      Currently, only ECS cloud-servers are supported, and BMS bare metal cloud-servers are not supported yet.
                    type: string
                  serverIdRef:
                    description: Reference to a Instance in ecs to populate serverId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serverIdSelector:
                    description: Selector for a Instance in ecs to populate serverId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  size:
                    description: |-
                      Specifies the disk size, in GB.
                      For system disk, the valid value ranges from 1 GB to 1,024 GB.
                      For data disk, the valid value ranges from 10 GB to 32,768 GB.
                    type: number
                  snapshotId:
                    description: Specifies the snapshot ID from which to create the
                      disk.
                    type: string
                  snapshotIdRef:
                    description: Reference to a Snapshot in evs to populate snapshotId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  snapshotIdSelector:
                    description: Selector for a Snapshot in evs to populate snapshotId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Specifies the key/value pairs to associate with the
                      disk.
                    type: object
                    x-kubernetes-map-type: granular
                  throughput:
                    description: |-
                      Specifies the throughput for the volume. The Unit is MiB/s.
                      The field is valid and required when volume_type is set to GPSSD2.
                      This field can be changed only when the disk status is Available or In-use.
                    type: number
                  volumeType:
                    description: 'Specifies the disk type. Valid values are as follows:'
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoPay:
                    type: string
                  autoRenew:
                    description: |-
                      Specifies whether auto-renew is enabled.
                      Valid values are true and false. Defaults to false.
                    type: string
                  availabilityZone:
                    description: Specifies the availability zone for the disk.
                    type: string
                  backupId:
                    description: Specifies the backup ID from which to create the
                      disk.
                    type: string
                  cascade:
                    description: |-
                      Specifies the delete mode of snapshot. The default value is false. All snapshot
                      associated with the disk will also be deleted when the parameter is set to true.
                    type: boolean
                  chargingMode:
                    description: |-
                      Specifies the charging mode of the disk.
                      The valid values are as follows:
                    type: string
                  dedicatedStorageId:
                    description: Specifies the ID of the DSS storage pool accommodating
                      the disk.
                    type: string
                  description:
                    description: Specifies the disk description. You can enter up
                      to 85 characters.
                    type: string
                  deviceType:
                    description: |-
                      Specifies the device type of disk to create. Valid options are VBD and
                      SCSI. Defaults to VBD.
                    type: string
                  enterpriseProjectId:
                    description: |-
                      Specifies the enterprise project ID of the disk.
                      For enterprise users, if omitted, default enterprise project will be used.
                    type: string
                  imageId:
                    description: Specifies the image ID from which to create the disk.
                    type: string
                  iops:
                    description: |-
                      Specifies the IOPS(Input/Output Operations Per Second) for the volume.
                      The field is valid and required when volume_type is set to GPSSD2 or ESSD2.
                      This field can be changed only when the disk status is Available or In-use.
                    type: number
                  kmsId:
                    description: Specifies the Encryption KMS ID to create the disk.
                    type: string
                  multiattach:
                    description: |-
                      be-expanded disk is in-use, the status of the server to which the disk attached
                      must be ACTIVE, PAUSED, SUSPENDED, or SHUTOFF.
                      5. Please refer to Notes and Constraints
                      to view the limitations of disk capacity expansion.
                    type: boolean
                  name:
                    description: Specifies the disk name. You can enter up to 64 characters.
                    type: string
                  period:
                    description: Specifies the charging period of the disk.
                    type: number
                  periodUnit:
                    description: |-
                      Specifies the charging period unit of the disk.
                      Valid values are month and year. This parameter is mandatory if charging_mode is set to prePaid.
                    type: string
                  region:
                    description: |-
                      Specifies the region in which to create the disk. If omitted, the
                      provider-level region will be used. Changing this parameter will create a new resource.
                    type: string
                  serverId:
                    description: |-
                      Specifies the server ID to which the cloud volume is to be mounted.
                      After specifying the value of this field, the cloud volume will be automatically attached on the cloud server.
                      The charging_mode of the created cloud volume will be consistent with that of the cloud server.
                      Currently, only ECS cloud-servers are supported, and BMS bare metal cloud-servers are not supported yet.
                    type: string
                  serverIdRef:
                    description: Reference to a Instance in ecs to populate serverId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  serverIdSelector:
                    description: Selector for a Instance in ecs to populate serverId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  size:
                    description: |-
                      Specifies the disk size, in GB.
                      For system disk, the valid value ranges from 1 GB to 1,024 GB.
                      For data disk, the valid value ranges from 10 GB to 32,768 GB.
                    type: number
                  snapshotId:
                    description: Specifies the snapshot ID from which to create the
                      disk.
                    type: string
                  snapshotIdRef:
                    description: Reference to a Snapshot in evs to populate snapshotId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  snapshotIdSelector:
                    description: Selector for a Snapshot in evs to populate snapshotId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Specifies the key/value pairs to associate with the
                      disk.
                    type: object
                    x-kubernetes-map-type: granular
                  throughput:
                    description: |-
                      Specifies the throughput for the volume. The Unit is MiB/s.
                      The field is valid and required when volume_type is set to GPSSD2.
                      This field can be changed only when the disk status is Available or In-use.
                    type: number
                  volumeType:
                    description: 'Specifies the disk type. Valid values are as follows:'
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.availabilityZone is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.availabilityZone)
                || (has(self.initProvider) && has(self.initProvider.availabilityZone))'
            - message: spec.forProvider.volumeType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.volumeType)
                || (has(self.initProvider) && has(self.initProvider.volumeType))'
          status:
            description: VolumeStatus defines the observed state of Volume.
            properties:
              atProvider:
                properties:
                  allMetadata:
                    additionalProperties:
                      type: string
                    description: 'The key-value pair disk metadata. Valid key-value
                      pairs are as follows:'
                    type: object
                    x-kubernetes-map-type: granular
                  allVolumeImageMetadata:
                    additionalProperties:
                      type: string
                    description: The metadata of the disk image.
                    type: object
                    x-kubernetes-map-type: granular
                  attachment:
                    description: |-
                      If a disk is attached to an instance, this attribute will display the attachment ID, instance ID, and
                      the device as the instance sees it. The attachment structure is documented below.
                    items:
                      properties:
                        attachedAt:
                          description: The time when the disk was attached.
                          type: string
                        attachedVolumeId:
                          description: The ID of the attached disk.
                          type: string
                        device:
                          description: The device name.
                          type: string
                        hostName:
                          description: The name of the physical host housing the cloud
                            server to which the disk is attached.
                          type: string
                        id:
                          description: The ID of the attachment information.
                          type: string
                        instanceId:
                          description: The ID of the server to which the disk is attached.
                          type: string
                        volumeId:
                          description: The disk ID.
                          type: string
                      type: object
                    type: array
                  autoPay:
                    type: string
                  autoRenew:
                    description: |-
                      Specifies whether auto-renew is enabled.
                      Valid values are true and false. Defaults to false.
                    type: string
                  availabilityZone:
                    description: Specifies the availability zone for the disk.
                    type: string
                  backupId:
                    description: Specifies the backup ID from which to create the
                      disk.
                    type: string
                  bootable:
                    description: 'Whether the disk is bootable. true: The disk is
                      bootable. false: The disk is not bootable.'
                    type: string
                  cascade:
                    description: |-
                      Specifies the delete mode of snapshot. The default value is false. All snapshot
                      associated with the disk will also be deleted when the parameter is set to true.
                    type: boolean
                  chargingMode:
                    description: |-
                      Specifies the charging mode of the disk.
                      The valid values are as follows:
                    type: string
                  createdAt:
                    description: The time when the disk was created.
                    type: string
                  dedicatedStorageId:
                    description: Specifies the ID of the DSS storage pool accommodating
                      the disk.
                    type: string
                  dedicatedStorageName:
                    description: The name of the DSS storage pool accommodating the
                      disk.
                    type: string
                  description:
                    description: Specifies the disk description. You can enter up
                      to 85 characters.
                    type: string
                  deviceType:
                    description: |-
                      Specifies the device type of disk to create. Valid options are VBD and
                      SCSI. Defaults to VBD.
                    type: string
                  enterpriseProjectId:
                    description: |-
                      Specifies the enterprise project ID of the disk.
                      For enterprise users, if omitted, default enterprise project will be used.
                    type: string
                  id:
                    description: The resource ID in UUID format.
                    type: string
                  imageId:
                    description: Specifies the image ID from which to create the disk.
                    type: string
                  iops:
                    description: |-
                      Specifies the IOPS(Input/Output Operations Per Second) for the volume.
                      The field is valid and required when volume_type is set to GPSSD2 or ESSD2.
                      This field can be changed only when the disk status is Available or In-use.
                    type: number
                  iopsAttribute:
                    description: |-
                      The disk IOPS information. This attribute appears only for a general purpose SSD V2 or an extreme
                      SSD V2 disk. The iops_attribute structure is documented below.
                    items:
                      properties:
                        frozened:
                          description: The frozen tag.
                          type: boolean
                        id:
                          description: The ID of the disk IOPS.
                          type: string
                        totalVal:
                          description: The IOPS.
                          type: number
                      type: object
                    type: array
                  kmsId:
                    description: Specifies the Encryption KMS ID to create the disk.
                    type: string
                  links:
                    description: |-
                      The disk URI.
                      The links structure is documented below.
                    items:
                      properties:
                        href:
                          description: The corresponding shortcut link.
                          type: string
                        rel:
                          description: The shortcut link marker name.
                          type: string
                      type: object
                    type: array
                  multiattach:
                    description: |-
                      be-expanded disk is in-use, the status of the server to which the disk attached
                      must be ACTIVE, PAUSED, SUSPENDED, or SHUTOFF.
                      5. Please refer to Notes and Constraints
                      to view the limitations of disk capacity expansion.
                    type: boolean
                  name:
                    description: Specifies the disk name. You can enter up to 64 characters.
                    type: string
                  period:
                    description: Specifies the charging period of the disk.
                    type: number
                  periodUnit:
                    description: |-
                      Specifies the charging period unit of the disk.
                      Valid values are month and year. This parameter is mandatory if charging_mode is set to prePaid.
                    type: string
                  region:
                    description: |-
                      Specifies the region in which to create the disk. If omitted, the
                      provider-level region will be used. Changing this parameter will create a new resource.
                    type: string
                  serialNumber:
                    description: |-
                      The disk serial number. This field is returned only for non-HyperMetro SCSI disks and is used for
                      disk mapping in the VM.
                    type: string
                  serverId:
                    description: |-
                      Specifies the server ID to which the cloud volume is to be mounted.
                      After specifying the value of this field, the cloud volume will be automatically attached on the cloud server.
                      The charging_mode of the created cloud volume will be consistent with that of the cloud server.
                      Currently, only ECS cloud-servers are supported, and BMS bare metal cloud-servers are not supported yet.
                    type: string
                  serviceType:
                    description: The service type. Supported services are EVS, DSS,
                      and DESS.
                    type: string
                  size:
                    description: |-
                      Specifies the disk size, in GB.
                      For system disk, the valid value ranges from 1 GB to 1,024 GB.
                      For data disk, the valid value ranges from 10 GB to 32,768 GB.
                    type: number
                  snapshotId:
                    description: Specifies the snapshot ID from which to create the
                      disk.
                    type: string
                  status:
                    description: |-
                      The disk status.
                      Please refer to EVS Disk Status.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Specifies the key/value pairs to associate with the
                      disk.
                    type: object
                    x-kubernetes-map-type: granular
                  throughput:
                    description: |-
                      Specifies the throughput for the volume. The Unit is MiB/s.
                      The field is valid and required when volume_type is set to GPSSD2.
                      This field can be changed only when the disk status is Available or In-use.
                    type: number
                  throughputAttribute:
                    description: |-
                      The disk throughput information. This attribute appears only for a general purpose SSD V2 disk.
                      The throughput_attribute structure is documented below.
                    items:
                      properties:
                        frozened:
                          description: The frozen tag.
                          type: boolean
                        id:
                          description: The throughput ID.
                          type: string
                        totalVal:
                          description: The throughput.
                          type: number
                      type: object
                    type: array
                  updatedAt:
                    description: The time when the disk was updated.
                    type: string
                  volumeType:
                    description: 'Specifies the disk type. Valid values are as follows:'
                    type: string
                  wwn:
                    description: The unique identifier used for mounting the EVS disk.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
